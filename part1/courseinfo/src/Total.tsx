const Total = (props: { exercises1: any; exercises2: any; exercises3: any }) => {
    return <p>Number of exercises {props.exercises1 + props.exercises2 + props.exercises3}</p>
}

export default Total
